name: Tailscale Test

on:
  push:
    branches:
      - '*'

  pull_request:
      branches:
        - '*'

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - name: Tailscale
        uses: tailscale/github-action@v2
        with:
          authkey: tskey-auth-kFrkrLM8kT11CNTRL-ukciTVi2UngFZUDkq9ddngcKmj3MK3JvV

      - name: status
        run: tailscale status